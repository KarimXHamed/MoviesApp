 fetchPoint { point, error in
        if let error = error {
            print("Failed to fetch point: \(error.localizedDescription)")
        } else if let point = point {
            print("Question: \(point.question), Answer: \(point.answer)")
        }
    }
