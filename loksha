   fetchLanguageModelName { name in
            if let name = name {
                print("Fetched name: \(name)")
                // You can update your UI or perform other actions with the fetched name here
            } else {
                print("Failed to fetch name.")
            }
        }
