# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LAnguageLearningApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

Pod 'Firebase/Core'
Pod 'Firebase/Database'

  target 'LAnguageLearningAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LAnguageLearningAppUITests' do
    # Pods for testing
  end

end
